server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agms_zone_db?createDatabaseIfNotExist=true
    username: root
    # Password එකේ special characters තියෙන නිසා අනිවාර්යයෙන්ම quote (" ") ඇතුළේ දාන්න
    password: "#Shanu0609@"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/