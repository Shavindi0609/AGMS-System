server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/agms_sensor_db?createDatabaseIfNotExist=true
    username: root
    password: "#Shanu0609@"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0 # හැම request එකක්ම trace කරන්න
  zipkin:
    tracing:
      endpoint: "http://localhost:9411/api/v2/spans"